# WP-012: Verification — Linux end-to-end build + ctest (success criteria)

- **Status:** open
- **Phase:** 4 — Verification & docs
- **Priority:** P0 (this is the acceptance gate for the whole effort)
- **Difficulty:** medium
- **Depends on:** WP-001 … WP-011 (all)
- **Plan ref:** PLAN.md §Phase 0 (success criteria), §Phase 4
- **GitHub issue:** #12

## Context

Phase 0 defines success as:

```
cmake -S . -B build && cmake --build build && ctest --test-dir build --output-on-failure
```

passing on Linux (Ubuntu 22.04+, GCC 12+ and Clang 15+) for the Application module and
its tests, with the Windows/MSVC build kept green throughout. Decision from Phase 0:
keep the shared-library build (matching `WP_APPLICATION_API` visibility attributes) —
no static-lib mode work.

## Changes

- [x] On Ubuntu 22.04+: install prerequisites (cmake ≥ 3.25, g++ 12+ / clang++ 15+,
      `libgl1-mesa-dev`, git submodules including MPP's nested deps).
- [x] Run configure + build + `ctest` with GCC; fix fallout.
- [x] Repeat with Clang; fix fallout.
- [ ] Re-run the Windows/MSVC multi-config build (Debug/Release/MemCheck) + `ctest`; confirm no regression.

## Verification

- GCC 15.2.0 / CMake 4.2.3: clean configure and build, 24/24 tests passed.
- Clang 21.1.8 / CMake 4.2.3: clean configure and build, 24/24 tests passed.
- Integration fallout fixed: single-config generators now default to `Release`, so the
  Phase 0 no-options configure command resolves MPP's configuration-specific artifacts.
- Windows/MSVC verification remains pending on a Windows host.

## Files

- No code changes expected; this ticket absorbs integration fallout from the other tickets.

## Acceptance criteria

- The success-criteria command passes on Linux with GCC **and** Clang (all ctest tests green).
- Windows/MSVC build + tests remain green.
- `willpower.viz` and other non-Application modules are out of scope for this pass
      (see follow-ups in `tickets/README.md`).
