# WP-007: Application — Scheduler unit test (task ordering + budget scaling)

- **Status:** done (commit 3711212)
- **Phase:** 2 — Willpower.Application sources
- **Priority:** P0 (guards the timing-rewrite from behaviour drift)
- **Difficulty:** medium
- **Depends on:** WP-005
- **Plan ref:** PLAN.md §Phase 2, item 3
- **GitHub issue:** #7

## Context

The QPC → `steady_clock` rewrite (WP-005) touches timing-sensitive µs budget logic.
The plan calls for a Scheduler unit test verifying task ordering and budget scaling so
the rewrite is covered on both platforms.

## Changes

- [x] Add a Scheduler test mirroring the existing test layout in `willpower.application/tests`
      (e.g. `tests/scheduler_tests.cpp`).
- [x] Cover task ordering across timeslices.
- [x] Cover budget scaling (a task over its µs budget gets scaled back; a task under budget is unaffected).
- [x] Register the test in the module CMake alongside the existing tests.

## Files

- `willpower.application/tests/scheduler_tests.cpp` (new)
- `willpower.application/CMakeLists.txt` (test registration)

## Acceptance criteria

- Test passes on Linux (GCC + Clang) and Windows (MSVC) under `ctest`.
- The test is deterministic (no real-time sleeps that could flake; fake/controllable clock if the helper allows it).
