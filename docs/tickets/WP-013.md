# WP-013: CI — add a Linux job (GCC and Clang)

- **Status:** open
- **Phase:** 4 — Verification & docs
- **Priority:** P1
- **Difficulty:** easy
- **Depends on:** WP-012 (locally verified first)
- **Plan ref:** PLAN.md §Phase 4, item 1
- **GitHub issue:** #13

## Context

The repo has no `.github/` CI configuration yet. The plan calls for a Linux CI job
running configure + build + `ctest`, while keeping the Windows job green.

## Changes

- [ ] Add a Linux CI job (Ubuntu 22.04+) running
      `cmake -S . -B build && cmake --build build && ctest --test-dir build --output-on-failure`
      with both GCC and Clang (two jobs or a matrix).
- [ ] Install prerequisites in the job: cmake ≥ 3.25, `libgl1-mesa-dev`,
      `git clone --recurse-submodules` (MPP nested deps included).
- [ ] Keep/add the equivalent Windows (MSVC) job with the same commands.

## Files

- `.github/workflows/ci.yml` (new)

## Acceptance criteria

- The Linux job enforces the Phase 0 success criteria on push/PR.
- The Windows job remains green.
